name: Y·NEX Desktop Desktop Build

on:
  push:
    paths:
      - 'download/source/**'
      - 'download/ynex-desktop-build/**'
      - 'download/package.json'
  workflow_dispatch:
    inputs:
      target:
        description: 'Build target (win/mac/linux/all)'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - win
          - mac
          - linux

jobs:
  build-win:
    name: Build Windows
    if: github.event_name != 'workflow_dispatch' || inputs.target == 'all' || inputs.target == 'win'
    runs-on: windows-latest
    defaults:
      run:
        working-directory: download/ynex-desktop-build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: npm install
      - name: Sync source files
        run: |
          Copy-Item -Path ..\source\index.html -Destination .\ -Force
          Copy-Item -Path ..\source\main.js -Destination .\ -Force
          Copy-Item -Path ..\source\preload.js -Destination .\ -Force
          Copy-Item -Path ..\source\css\ -Destination .\css\ -Recurse -Force
          Copy-Item -Path ..\source\js\ -Destination .\js\ -Recurse -Force
      - name: Build
        run: npm run dist:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Y·NEX Desktop-Windows
          path: download/ynex-desktop-build/release/Y·NEX Desktop-*-win-Setup.exe

  build-mac:
    name: Build macOS
    if: github.event_name != 'workflow_dispatch' || inputs.target == 'all' || inputs.target == 'mac'
    runs-on: macos-latest
    defaults:
      run:
        working-directory: download/ynex-desktop-build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: npm install
      - name: Sync source files
        run: |
          cp ../source/index.html ./
          cp ../source/main.js ./
          cp ../source/preload.js ./
          cp -r ../source/css/* ./css/
          cp -r ../source/js/* ./js/
      - name: Build
        run: npm run dist:mac
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Y·NEX Desktop-macOS
          path: download/ynex-desktop-build/release/Y·NEX Desktop-*-mac.dmg

  build-linux:
    name: Build Linux
    if: github.event_name != 'workflow_dispatch' || inputs.target == 'all' || inputs.target == 'linux'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: download/ynex-desktop-build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: npm install
      - name: Sync source files
        run: |
          cp ../source/index.html ./
          cp ../source/main.js ./
          cp ../source/preload.js ./
          cp -r ../source/css/* ./css/
          cp -r ../source/js/* ./js/
      - name: Build
        run: npm run dist:linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Y·NEX Desktop-Linux
          path: download/ynex-desktop-build/release/Y·NEX Desktop-*-linux.*
